# Pacote "schemas": schemas Pydantic de entrada/saida da API
# (Etapa 1, secao 1.6). Separado de modelos/ de proposito — modelos/
# e o desenho de tabela do banco, schemas/ e o formato que trafega
# na API. Nem sempre coincidem (ex.: TokenResponse nao existe como
# tabela nenhuma).
