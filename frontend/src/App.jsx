// Componente raiz temporario.
// Vai virar o roteador entre as paginas (login, agenda, clientes,
// servicos — Etapa 1, secao 1.6) quando src/paginas/ existir e a
// Etapa 3 (autenticacao) definir a tela de login.

export default function App() {
  return <p>Projeto Agenda — em construcao.</p>;
}
